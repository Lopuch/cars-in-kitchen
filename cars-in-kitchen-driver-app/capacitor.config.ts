import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cars-in-kitchen-driver',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
